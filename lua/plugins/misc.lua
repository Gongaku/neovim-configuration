require("ibl").setup()
require("focus").setup()

if not helpers.is_work then
  -- require("typst-preview").setup({
  --   dependencies_bin = {
  --     tinymist = 'tinymist',
  --     websocat = 'websocat'
  --   },
  -- })
end
